[build]
  command = "cd core-contract && npx hardhat compile && cd .. && npm run build"
  publish = "dist"

# Uncomment and customize the following if you need redirects or headers
#[[redirects]]
#  from = "/*"
#  to = "/index.html"
#  status = 200
